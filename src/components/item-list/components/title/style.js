import styled from 'styled-components'

// Constants
import { Colors } from '../../../../utils/Colors'

export const SCTitle = styled.p`
	color: ${Colors.black};
	font-weight: 500;
	font-size: 18px;
  margin: 0;
`
